alias c='clear'
alias s='source ~/.zshrc'
#alias vim=nvim
alias v=vim
alias e='nvim .'
alias pkglist="rm -f ~/Dev/paclist.txt && paru -Qqe > ~/Dev/paclist.txt"

# Commandes de base
alias cp='cp --interactive'
alias mv='mv --interactive'
alias rm='rm --interactive'
#alias pwd='pwd && pwd | xclip -sel primary'
alias less='less --quiet'
alias ..='cd ..'
alias df='df --human-readable'
alias du='du --human-readable'

# Gestion du temps
alias date-computer='date +"%Y-%m-%d_%H:%M:%S"'
alias date-human='date +"%A %d %B %Y, %H:%M"'

# Gestion des fichiers et répertoires avec 'exa' et 'find'
alias ls='exa --group-directories-first --icons'
alias la='ls -a'
alias ll='ls -l'
alias lla='ls -la'
alias ldot='ls -ld .*'

# Compression et décompression
alias untar='tar xvf'

alias z="zellij attach || zellij"
alias zl="zellij list-sessions"
alias zs="zellij --session"
# Git
alias lg="lazygit"
alias g="git status"
alias ga="git add"
alias gaa="git add ."
alias gau="git add -u"
alias gc="git commit -m"
alias gca="git commit -am"
alias gb="git branch"
alias gbd="git branch -d"
alias gco="git checkout"
alias gcob="git checkout -b"
alias gt="git stash"
alias gta="git stash apply"
alias gm="git merge"
alias gr="git rebase"
alias gl="git log --oneline --decorate --graph"
alias glog="git log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --"
alias glga="git log --graph --oneline --all --decorate"
alias gs="git show"
alias gd="diff --color --color-words --abbrev"
alias gdc="git diff --cached"
alias gbl="git blame"
alias gps="git push"
alias gpl="git pull"
alias gk="gitk --all&"
alias gx="gitx --all"
alias gx="ign = ls-files -o -i --exclude-standard"
alias grmc="git rm -r --cached"

# Pacman (Arch Linux)
alias pu='paru'
alias pi='paru -Si'
alias pr='paru -Rs'
alias pl='pacman -Q'
alias ple='pacman -Qe'
alias pla='pacman -Qem'
alias pc='sudo pacman -Scc'
alias pf="paruz"

# Apt (Debian/Ubuntu)
alias au='sudo apt update && sudo apt upgrade && sudo apt dist-upgrade && sudo apt autoremove && sudo apt autoclean && sudo apt clean && flatpak update'
alias ai="sudo apt install"
alias ap="sudo apt purge"
alias ar="sudo apt remove"
alias as="apt search"

# Flatpak
alias fp='flatpak'

# swap esc & caps lock
alias se="setxkbmap -option caps:swapescape"
alias sb="setxkbmap -option"

# shortcuts & quick navigation
alias dp="vim $DOTPATH"
alias cf="cd $DOTPATH"

# Python
alias venv-mk="python3.12 -m venv venv"
alias venv-activate="source venv/bin/activate"

# pbcopy/paste like
alias cbcopy='xsel --clipboard --input'
alias cbpaste='xsel --clipboard --output'
